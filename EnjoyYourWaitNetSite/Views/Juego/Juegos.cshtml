@{
    ViewBag.Title = "Juegos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2 class="titulo-secundario">@ViewBag.Title</h2>

<div class="row">
    <div class="col-md-4 col-juegos">
        <button class="carta-juego" onclick="location.href='@Url.Action("Memotest", "Juego")'">Memotest</button>
    </div>
    <div class="col-md-4 col-juegos">
        <button class="carta-juego" onclick="location.href='@Url.Action("Ruleta", "Juego")'">Ruleta</button>
    </div>
    <div class="col-md-4 col-juegos">
        <p><i>Proximamente más juegos.</i></p>
    </div>
</div>
